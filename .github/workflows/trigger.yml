name: Trigger Project Math

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  calculate:
    uses: intellicloud/.github/.github/workflows/calculate-fields.yml@main
    secrets:
      ORG_APP_ID: ${{ secrets.ORG_APP_ID }}
      ORG_APP_PRIVATE_KEY: ${{ secrets.ORG_APP_PRIVATE_KEY }}
